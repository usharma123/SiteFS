# @sitefs/axfs

Maps CDP accessibility trees to a virtual filesystem (`ls`, `cd`, `find`, `grep`) and provides AX filesystem diff (`diffAxFilesystem`).

Separate from evidence snapshot diff in `@sitefs/sitefs`.
