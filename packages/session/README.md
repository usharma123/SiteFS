# @sitefs/session

Orchestrates a SiteFS session: `LocalSiteFSStore`, `WorkerBrowserBackend`, `WebRuntime`, and `BrowserHost`.

Use `createSessionContext()` / `closeSessionContext()` from CLI, MCP, and tests.
