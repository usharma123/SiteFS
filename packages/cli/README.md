# sitefs (CLI)

Thin application entrypoints: `shell`, `mcp`, `test`, `view`, `doctor`, `demo`.

Session wiring is delegated to `@sitefs/session`; live commands to `@sitefs/live`; web QA to `WebRuntime` in session.
