# @sitefs/live

Live accessibility shell: `BrowserHost` dispatches tab, AX, interaction, and automation commands against the in-memory AX tree.

Depends on `@sitefs/axfs`, `@sitefs/browser`, `@sitefs/commands`, and `@sitefs/sitefs` (types/store).
