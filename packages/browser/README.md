# @sitefs/browser

Playwright-backed `BrowserBackend` and the stdio JSON-RPC browser worker subprocess (`WorkerBrowserBackend`, `startBrowserWorker`).

Must not depend on `@sitefs/axfs` or CLI packages.
