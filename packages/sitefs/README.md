# @sitefs/sitefs

Persistence layer: session config, `LocalSiteFSStore`, snapshot/history I/O, snapshot diff, registry, and viewer manifest builders.

Public API is curated in `src/index.ts`; see `ARCHITECTURE.md` for slug and path conventions.
